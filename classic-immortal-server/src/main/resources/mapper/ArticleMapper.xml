<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.classicimmortalserver.mapper.ArticleMapper">



    <select id="selectAll" resultType="com.example.classicimmortalserver.entity.Article">

SELECT * FROM `classic_immortal`.article
<where >
    <if test="title!=null"> name like concat('%',#{name},'%')</if>
</where>
order by id desc
    </select>



    <insert id="insert">
       insert into `classic_immortal`.article (title, img, description, content, time)
       values (#{title},#{img},#{description},#{content},#{time})

    </insert>


    <update id="updateById" parameterType="com.example.classicimmortalserver.entity.Article">
        update `classic_immortal`.article set title=#{title},img=#{img},description=#{description},
                                              content=#{content},time=#{time}

        where id=#{id}


    </update>





















</mapper>
